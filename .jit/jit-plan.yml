name: My plan
level: beginner
author: Jit
version: 0.1
owners:
  default: null
references:
  - https://www.jit.io
tags: []
items:
  - name: Scan your code for vulnerabilities (SAST)
    uses: >-
      jitsecurity-controls/jit-plans/items/code/item-code-vulnerability.yml@latest
  - name: Scan code for hard-coded secrets
    uses: jitsecurity-controls/jit-plans/items/code/item-secret-detection.yml@latest

override:
  workflows:
    the actual workflow that i want to override
    sast:
      jobs:
        static-code-analysis-python-semgrep:
          steps:
          - name: Run semgrep python
            uses: ${{ context.config.custom_registry || registry.jit.io }}/control-semgrep-alpine:latest
            with:
              args: some-deadly-args --vibe vacay --name Thailand
        secret-detection:
          jobs:
            secret-detection:
              steps:
                - name: Run Gitleaks
                  # uses: ${{ context.config.custom_registry || registry.jit.io }}/control-gitleaks-alpine:latest
                  with:
                    args: some-deadly-args --vibe vacay --name Thailand
                  # args: detect --config $GITLEAKS_CONFIG_FILE_PATH --source ${WORK_DIR:-.} -v --report-format json --report-path $REPORT_FILE --redact --no-git --exit-code 0
